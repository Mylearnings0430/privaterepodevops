version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto8
  pre_build:
    commands:
    - echo In the pre_build phase...
  build:
    commands:
    - echo Build started for Spring boot app
    - mvn install
  post_build:
    commands:
    - echo Post Build Started..
artifacts:
  files:
  - target/webapp-0.0.1-SNAPSHOT.jar
